package com.qs.image_packager.jdbc;

import static org.junit.Assert.assertNotNull;

import org.junit.Test;

public class ImageDownloadJobsRowMapperTest {
	
	private ImageDownloadJobsRowMapper mapper = new ImageDownloadJobsRowMapper() ;

	@Test
	public void test() {
		assertNotNull(mapper) ;
		// TODO: Add mocking for the result set and test that we got back what we expected.
	}

}
